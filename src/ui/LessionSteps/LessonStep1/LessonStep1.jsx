// ui/LessonStep1.jsx
export const LessonStep1 = () => (
  <div>
    <h2>1. Как устроен процесс рендеринга в React</h2>
    <p>
      React сначала вызывает функцию-компоненту и создаёт виртуальное дерево
      элементов. Когда состояние или пропсы меняются — React сравнивает новое
      дерево с предыдущим (diffing) и обновляет только изменившиеся части DOM.
    </p>
  </div>
);
